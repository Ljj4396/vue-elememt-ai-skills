# 主题系统实施计划 - 三种风格主题

> 创建时间：2026-02-04
> 目标：实现运势玄学、AI 科技、混合平衡三种主题风格

---

## 📋 目标

为系统添加三种可切换的主题风格，每个账号可以选择不同的样式，契合运气和智能的核心理念。

---

## 🎨 三种主题方案

### 主题 1：运势玄学（Fortune Mystic）

**设计理念：** 东方神秘美学 + 占卜元素

**配色方案：**
```css
--theme-primary: #ffc53d        /* 金色 */
--theme-secondary: #ff4d4f      /* 红色 */
--theme-accent: #ff7a45         /* 橙色 */
--theme-bg-primary: #1a0a0f     /* 深红黑 */
--theme-bg-secondary: #2a1a1f   /* 深红紫 */
--theme-text: #fff
--theme-text-secondary: rgba(255, 255, 255, 0.7)
--theme-border: rgba(255, 197, 61, 0.3)
--theme-shadow: rgba(255, 77, 79, 0.3)
```

**视觉特点：**
- 🎴 卡片带有金色边框和阴影
- ⭐ 星光粒子背景动画
- 🔮 水晶球风格的加载动画
- 📜 卷轴展开过渡效果
- 🌟 运势提示浮窗

---

### 主题 2：AI 科技（AI Futuristic）

**设计理念：** 赛博朋克 + 未来科技

**配色方案：**
```css
--theme-primary: #00d9ff        /* 电光蓝 */
--theme-secondary: #8b5cf6      /* 紫色 */
--theme-accent: #ec4899         /* 品红 */
--theme-bg-primary: #0a0a1f     /* 深蓝黑 */
--theme-bg-secondary: #1a1a2f   /* 深蓝紫 */
--theme-text: #e0e7ff
--theme-text-secondary: rgba(224, 231, 255, 0.7)
--theme-border: rgba(0, 217, 255, 0.3)
--theme-shadow: rgba(139, 92, 246, 0.3)
```

**视觉特点：**
- 💠 六边形网格背景
- ⚡ 霓虹线条和发光效果
- 🤖 AI 助手悬浮球
- 🌐 数字雨动画
- 🔵 玻璃拟态卡片

---

### 主题 3：混合平衡（Harmony Balance）

**设计理念：** 东西方融合 + 阴阳平衡

**配色方案：**
```css
--theme-primary: #8b5cf6        /* 紫色（科技） */
--theme-secondary: #fbbf24      /* 金色（玄学） */
--theme-accent: #06b6d4         /* 青色 */
--theme-bg-primary: #1e1b4b     /* 深紫 */
--theme-bg-secondary: #312e81   /* 中紫 */
--theme-text: #f3f4f6
--theme-text-secondary: rgba(243, 244, 246, 0.7)
--theme-border: rgba(139, 92, 246, 0.3)
--theme-shadow: rgba(251, 191, 36, 0.3)
```

**视觉特点：**
- ☯️ 太极图标和旋转动画
- 🌊 能量波纹效果
- ⚛️ 原子轨道装饰
- 🎭 冷暖色调渐变
- 🌌 星空粒子背景

---

## 📐 实施计划

### 任务 1：创建主题配置系统

**文件：** `src/stores/theme.js`

**功能：**
1. 定义三种主题配置对象
2. 管理当前主题状态
3. 提供主题切换方法
4. 本地存储持久化

**代码结构：**
```javascript
import { defineStore } from 'pinia'

export const useThemeStore = defineStore('theme', {
  state: () => ({
    currentTheme: 'default', // 'fortune', 'ai', 'harmony'
    themes: {
      default: { /* 当前紫色主题 */ },
      fortune: { /* 运势玄学主题 */ },
      ai: { /* AI 科技主题 */ },
      harmony: { /* 混合平衡主题 */ }
    }
  }),
  actions: {
    setTheme(themeName) {
      this.currentTheme = themeName
      this.applyTheme()
      localStorage.setItem('user-theme', themeName)
    },
    applyTheme() {
      const theme = this.themes[this.currentTheme]
      Object.keys(theme).forEach(key => {
        document.documentElement.style.setProperty(key, theme[key])
      })
    },
    loadTheme() {
      const saved = localStorage.getItem('user-theme')
      if (saved && this.themes[saved]) {
        this.setTheme(saved)
      }
    }
  }
})
```

---

### 任务 2：创建主题选择页面

**文件：** `src/views/ThemeSelector.vue`

**布局：**
```
┌─────────────────────────────────────┐
│  🎨 主题选择                         │
│  选择您喜欢的界面风格                │
├─────────────────────────────────────┤
│  ┌───────┐  ┌───────┐  ┌───────┐   │
│  │运势玄学│  │AI科技 │  │混合平衡│   │
│  │预览图 │  │预览图 │  │预览图 │   │
│  │       │  │       │  │       │   │
│  │[选择] │  │[选择] │  │[选择] │   │
│  └───────┘  └───────┘  └───────┘   │
│                                     │
│  当前主题：运势玄学                  │
│  [应用主题]                         │
└─────────────────────────────────────┘
```

**功能：**
- 三个主题预览卡片
- 点击卡片高亮选中
- 实时预览效果
- 应用按钮切换主题

---

### 任务 3：实现运势玄学主题样式

**文件：** `src/assets/themes/fortune.css`

**关键样式：**

1. **背景动画**
```css
.theme-fortune {
  background: linear-gradient(135deg, #1a0a0f 0%, #0a0a0f 100%);
}

.theme-fortune::before {
  content: '';
  position: fixed;
  inset: 0;
  background: radial-gradient(circle at 20% 50%, rgba(255, 77, 79, 0.1) 0%, transparent 50%),
              radial-gradient(circle at 80% 50%, rgba(255, 197, 61, 0.1) 0%, transparent 50%);
  animation: fortune-glow 8s ease-in-out infinite;
}

@keyframes fortune-glow {
  0%, 100% { opacity: 0.5; }
  50% { opacity: 0.8; }
}
```

2. **卡片样式**
```css
.theme-fortune .card {
  background: rgba(42, 26, 31, 0.8);
  border: 2px solid rgba(255, 197, 61, 0.3);
  box-shadow: 0 8px 32px rgba(255, 77, 79, 0.2),
              inset 0 1px 1px rgba(255, 197, 61, 0.1);
}

.theme-fortune .card:hover {
  border-color: rgba(255, 197, 61, 0.6);
  box-shadow: 0 12px 48px rgba(255, 77, 79, 0.3),
              0 0 20px rgba(255, 197, 61, 0.4);
  transform: translateY(-4px);
}
```

3. **按钮样式**
```css
.theme-fortune .btn-primary {
  background: linear-gradient(135deg, #ffc53d, #ff7a45);
  border: none;
  box-shadow: 0 4px 16px rgba(255, 197, 61, 0.4);
}

.theme-fortune .btn-primary:hover {
  background: linear-gradient(135deg, #ffd666, #ff8c5a);
  box-shadow: 0 6px 24px rgba(255, 197, 61, 0.6);
}
```

4. **星光粒子**
```css
.theme-fortune .particles {
  position: fixed;
  inset: 0;
  pointer-events: none;
}

.theme-fortune .particle {
  position: absolute;
  width: 2px;
  height: 2px;
  background: #ffc53d;
  border-radius: 50%;
  animation: twinkle 3s ease-in-out infinite;
}

@keyframes twinkle {
  0%, 100% { opacity: 0; transform: scale(0); }
  50% { opacity: 1; transform: scale(1); }
}
```

---

### 任务 4：实现 AI 科技主题样式

**文件：** `src/assets/themes/ai.css`

**关键样式：**

1. **六边形网格背景**
```css
.theme-ai {
  background: linear-gradient(135deg, #0a0a1f 0%, #000000 100%);
}

.theme-ai::before {
  content: '';
  position: fixed;
  inset: 0;
  background-image:
    linear-gradient(30deg, rgba(0, 217, 255, 0.05) 12%, transparent 12.5%, transparent 87%, rgba(0, 217, 255, 0.05) 87.5%, rgba(0, 217, 255, 0.05)),
    linear-gradient(150deg, rgba(0, 217, 255, 0.05) 12%, transparent 12.5%, transparent 87%, rgba(0, 217, 255, 0.05) 87.5%, rgba(0, 217, 255, 0.05));
  background-size: 80px 140px;
  animation: grid-move 20s linear infinite;
}

@keyframes grid-move {
  0% { background-position: 0 0; }
  100% { background-position: 80px 140px; }
}
```

2. **霓虹卡片**
```css
.theme-ai .card {
  background: rgba(26, 26, 47, 0.6);
  border: 1px solid rgba(0, 217, 255, 0.3);
  box-shadow: 0 8px 32px rgba(139, 92, 246, 0.2),
              inset 0 1px 1px rgba(0, 217, 255, 0.1);
  backdrop-filter: blur(10px);
}

.theme-ai .card:hover {
  border-color: rgba(0, 217, 255, 0.6);
  box-shadow: 0 12px 48px rgba(139, 92, 246, 0.3),
              0 0 30px rgba(0, 217, 255, 0.4);
}
```

3. **霓虹按钮**
```css
.theme-ai .btn-primary {
  background: linear-gradient(135deg, #00d9ff, #8b5cf6);
  border: 1px solid rgba(0, 217, 255, 0.5);
  box-shadow: 0 4px 16px rgba(0, 217, 255, 0.4),
              inset 0 1px 1px rgba(255, 255, 255, 0.2);
}

.theme-ai .btn-primary:hover {
  background: linear-gradient(135deg, #33e1ff, #a78bfa);
  box-shadow: 0 6px 24px rgba(0, 217, 255, 0.6),
              0 0 20px rgba(0, 217, 255, 0.5);
}
```

4. **数字雨效果**
```css
.theme-ai .digital-rain {
  position: fixed;
  inset: 0;
  pointer-events: none;
  overflow: hidden;
}

.theme-ai .rain-column {
  position: absolute;
  top: -100%;
  font-family: 'Courier New', monospace;
  font-size: 14px;
  color: rgba(0, 217, 255, 0.6);
  animation: rain-fall 10s linear infinite;
}

@keyframes rain-fall {
  0% { top: -100%; opacity: 0; }
  10% { opacity: 1; }
  90% { opacity: 1; }
  100% { top: 100%; opacity: 0; }
}
```

---

### 任务 5：实现混合平衡主题样式

**文件：** `src/assets/themes/harmony.css`

**关键样式：**

1. **渐变背景**
```css
.theme-harmony {
  background: linear-gradient(135deg, #1e1b4b 0%, #0f172a 100%);
}

.theme-harmony::before {
  content: '';
  position: fixed;
  inset: 0;
  background: radial-gradient(circle at 30% 50%, rgba(139, 92, 246, 0.15) 0%, transparent 50%),
              radial-gradient(circle at 70% 50%, rgba(251, 191, 36, 0.15) 0%, transparent 50%);
  animation: harmony-pulse 10s ease-in-out infinite;
}

@keyframes harmony-pulse {
  0%, 100% { opacity: 0.6; }
  50% { opacity: 0.9; }
}
```

2. **双色调卡片**
```css
.theme-harmony .card {
  background: linear-gradient(135deg, rgba(49, 46, 129, 0.6) 0%, rgba(30, 27, 75, 0.6) 100%);
  border: 1px solid transparent;
  border-image: linear-gradient(135deg, rgba(139, 92, 246, 0.5), rgba(251, 191, 36, 0.5)) 1;
  box-shadow: 0 8px 32px rgba(139, 92, 246, 0.2);
}

.theme-harmony .card:hover {
  box-shadow: 0 12px 48px rgba(139, 92, 246, 0.3),
              0 0 20px rgba(251, 191, 36, 0.3);
}
```

3. **渐变按钮**
```css
.theme-harmony .btn-primary {
  background: linear-gradient(135deg, #8b5cf6, #fbbf24);
  border: none;
  box-shadow: 0 4px 16px rgba(139, 92, 246, 0.4);
  position: relative;
  overflow: hidden;
}

.theme-harmony .btn-primary::before {
  content: '';
  position: absolute;
  inset: 0;
  background: linear-gradient(135deg, #a78bfa, #fcd34d);
  opacity: 0;
  transition: opacity 0.3s;
}

.theme-harmony .btn-primary:hover::before {
  opacity: 1;
}
```

4. **太极动画**
```css
.theme-harmony .taiji-symbol {
  width: 100px;
  height: 100px;
  border-radius: 50%;
  background: linear-gradient(to right, #8b5cf6 50%, #fbbf24 50%);
  position: relative;
  animation: taiji-rotate 8s linear infinite;
}

.theme-harmony .taiji-symbol::before,
.theme-harmony .taiji-symbol::after {
  content: '';
  position: absolute;
  width: 50px;
  height: 50px;
  border-radius: 50%;
}

.theme-harmony .taiji-symbol::before {
  top: 0;
  left: 25px;
  background: radial-gradient(circle at 50% 50%, #fbbf24 25%, #8b5cf6 25%);
}

.theme-harmony .taiji-symbol::after {
  bottom: 0;
  left: 25px;
  background: radial-gradient(circle at 50% 50%, #8b5cf6 25%, #fbbf24 25%);
}

@keyframes taiji-rotate {
  0% { transform: rotate(0deg); }
  100% { transform: rotate(360deg); }
}
```

---

### 任务 6：集成主题到全局组件

**文件：** `src/App.vue`

**改动点：**

1. **引入主题 store**
```vue
<script setup>
import { onMounted } from 'vue'
import { useThemeStore } from '@/stores/theme'

const themeStore = useThemeStore()

onMounted(() => {
  themeStore.loadTheme()
})
</script>
```

2. **添加主题类名**
```vue
<template>
  <div :class="['app-container', `theme-${themeStore.currentTheme}`]">
    <AdminLayout v-if="showLayout">
      <router-view v-slot="{ Component }">
        <transition name="fade-slide" mode="out-in">
          <component :is="Component" />
        </transition>
      </router-view>
    </AdminLayout>
    <router-view v-else v-slot="{ Component }">
      <transition name="fade" mode="out-in">
        <component :is="Component" />
      </transition>
    </router-view>
  </div>
</template>
```

3. **引入主题样式**
```vue
<style>
@import './assets/themes/fortune.css';
@import './assets/themes/ai.css';
@import './assets/themes/harmony.css';

/* 主题切换过渡 */
.app-container {
  transition: background 0.5s ease, color 0.5s ease;
}
</style>
```

---

### 任务 7：添加路由和菜单入口

**文件：** `src/router/index.js`

**添加路由：**
```javascript
{
  path: '/theme',
  name: 'ThemeSelector',
  component: () => import('@/views/ThemeSelector.vue'),
  meta: { requiresAuth: true, title: '主题选择' }
}
```

**文件：** `src/components/AdminLayout.vue`

**添加菜单项：**
```javascript
const menuItems = [
  // ... 现有菜单
  {
    path: '/theme',
    name: 'ThemeSelector',
    title: '主题选择',
    icon: Setting
  },
]
```

---

### 任务 8：测试主题切换功能

**测试清单：**

1. **主题切换测试**
   - [ ] 切换到运势玄学主题
   - [ ] 切换到 AI 科技主题
   - [ ] 切换到混合平衡主题
   - [ ] 切换回默认主题

2. **持久化测试**
   - [ ] 刷新页面后主题保持
   - [ ] 退出登录后主题保持
   - [ ] 不同账号可以有不同主题

3. **页面兼容性测试**
   - [ ] 登录页
   - [ ] 仪表盘
   - [ ] 用户管理
   - [ ] AI 聊天
   - [ ] 运势卡片
   - [ ] 余额表
   - [ ] 权限管理

4. **动画效果测试**
   - [ ] 主题切换过渡流畅
   - [ ] 背景动画正常
   - [ ] 卡片悬停效果
   - [ ] 按钮交互反馈

5. **响应式测试**
   - [ ] 桌面端（1920px）
   - [ ] 笔记本（1366px）
   - [ ] 平板（768px）
   - [ ] 手机（375px）

---

## 🎯 预期效果

### 运势玄学主题
- 金红配色，神秘东方美学
- 星光粒子背景
- 卡片带有金色边框和阴影
- 适合查看运势、占卜功能

### AI 科技主题
- 蓝紫配色，赛博朋克风格
- 六边形网格背景
- 霓虹发光效果
- 适合使用 AI 聊天功能

### 混合平衡主题
- 紫金配色，东西方融合
- 太极元素和能量波纹
- 冷暖色调渐变
- 平衡运气和智能两大核心

---

## ✅ 验收标准

1. 三种主题完整实现，视觉效果符合设计
2. 主题切换流畅，有过渡动画
3. 主题选择持久化存储
4. 所有页面和组件兼容三种主题
5. 响应式设计在所有设备上正常
6. 性能良好，无卡顿

---

**计划制定完成，等待批准后开始实施。**
